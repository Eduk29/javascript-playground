_7f1‍.x([["somePrototype",()=>somePrototype]]);const somePrototype = (arrayInput, testFunction) => {
  if (!arrayInput || !testFunction) {
    return [];
  }

  return arrayInput.some(testFunction);
};
