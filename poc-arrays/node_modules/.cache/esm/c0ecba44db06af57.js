_db0‍.x([["findIndexPrototype",()=>findIndexPrototype]]);const findIndexPrototype = (arrayInput, testCondition) => {
  if (!arrayInput || arrayInput.lenght === 0) {
    return arrayInput;
  }

  return arrayInput.findIndex(testCondition);
};
