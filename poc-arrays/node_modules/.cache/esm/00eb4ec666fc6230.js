_66f‍.x([["slicePrototype",()=>slicePrototype]]);const slicePrototype = (arrayInput, beginPosition, endPosition) => {
  if (!arrayInput) {
    return [];
  }

  return arrayInput.slice(beginPosition, endPosition);
};
