_9c2‍.x([["unshiftPrototype",()=>unshiftPrototype]]);const unshiftPrototype = (arrayInput, itemsToAdd) => {
  if (!arrayInput || !itemsToAdd) {
    return [];
  }

  return arrayInput.unshift(itemsToAdd);
};
