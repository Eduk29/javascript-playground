_277‍.x([["reducePrototype",()=>reducePrototype]]);const reducePrototype = (arrayInput, reducerFunction) => {
  if (!arrayInput || !reducerFunction || !reducePrototype) {
    return [];
  }

  return arrayInput.reduce(reducerFunction);
};
